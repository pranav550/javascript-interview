let str = "code improve testmy last is test";
// str = str.indexOf("test");
// str = str.lastIndexOf("test");
//str = str.indexOf("test", 14);
//str = str.lastIndexOf("test", 15);

//str = str.slice(1, 3);  // start, end
//str = str.substring(1, 3);  // start. end
//str = str.slice(0, -3);
//str = str.slice(-3);
//str = str.slice(2);

//str = str.substr(0, 3); // start, length

//str = str.replace("test", "aaaa");
str = str.replaceAll("test", "aaaa");
console.log(str);
